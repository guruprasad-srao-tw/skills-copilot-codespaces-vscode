function skillsMember() {
  var member = document.getElementById('member').value;
  var member = member.toLowerCase();
  var member = member.replace(/[^a-zA-Z ]/g, "");
  var member = member.trim();
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.toLowerCase();
  var member = member.replace(/[^a-zA-Z ]/g, "");
  var member = member.trim();
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr) {
    return value !== "";
  });
  var member = member.join(" ");
  var member = member.split(" ");
  var member = member.filter(function(value, index, arr